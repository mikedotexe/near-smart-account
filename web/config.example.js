// Copy this file to `config.local.js` (gitignored) and paste your FastNEAR
// API key. The frontend auto-loads `config.local.js` on start if present;
// missing is fine (you'll see a 404 in devtools — harmless).
//
// The Config panel also exposes a key input; anything pasted there is saved
// to localStorage under `FASTNEAR_API_KEY` and overrides whatever's set here.
window.FASTNEAR_API_KEY = "";
